package enums.enumsForAudi;

import interfaces.Option;

public enum OptionForAudi implements Option {
    CONDITION,
    PARKTRONICS,
    PANORAMA,
    BACKWHEEL,
    GPS,
    MUSIC,
    FOGLIGHTS;
}
